# Follow-up Phase Seed — Structured Verdict Observability + Workaround Cleanup (untracked working note)

STATUS 2026-07-23: the structured-verdict-observability phase is COMPLETE AND MERGED (CM #65 = 0408e71, CME #15 = ea01f8e); this file now seeds the NEXT work: (1) Qdrant teardown hardening (user-confirmed first item), (2) v0.1.6 planning (absorbing the port cross-links and the deferral-reconfirmation checklist), plus the trims and exit-rubric deferrals below. Originally: Draft as a normal harness phase (plan-format, user approval, then dispatch). This file is untracked working state; delete once the phase plan absorbs it. Full finding bodies live in agmsg history (orchestrator inbox, 2026-07-20 21:58–22:23Z reports) and this session's transcript.

## Sources (four sweeps + review deferrals, all read-only, all acknowledged)

1. CME round 1 (evals-worker, at 4cbc1b0): 5 findings — write/lifecycle verdicts discarded before benchmark records (3 MAJOR: typed-ingest drop, explicit-commit Debug-flattening + path asymmetry, lifecycle maintenance-failure drop); retrieval telemetry DTO gaps (MEDIUM); untyped metrics Value silently dropped from summaries (MEDIUM).
2. CM round 1 (worker2, at a0dff33): 13 findings F1–F13 — F1 rejection half still lossy (success half fixed by 13bc56f); typed issue/warning enums (F2); trace identity gaps: vector surface (F3), link_id (F4); prose-only SectionAssignment.reason (F5); telemetry missing configured filters (F6); bounded-failure structured-in-trace/stringly-in-error (F7); lifecycle validation flattened at facade (F8); vector-maintenance failure joining (F9); stringly code/kind vocabularies (F10); collection-compat prose errors (F11); config error prose (F12); dormant reconciliation detail prose (F13).
3. CME round 2 (evals-worker, at 2277f7b): 9 findings — stringly domain DTOs forcing adapter conversion shim (MAJOR); vector_only reimplementing hidden raw-vector capability against CM's Qdrant payload schema (MAJOR); mixed pseudo-provider + create/restart constructor duplication; parallel manual dataset registry; dead namespace-reset knobs (SMALL pull-forward candidate); boolean retrieval flags hiding magic section budgets; divergent context-pack renderers (items/text/counts unowned); twice-implemented OpenAI embeddings client with unequal validation; copied atomic file-replacement helper (LOW).
4. CM round 2 (worker2, at 13bc56f): 16 findings R2-01–R2-16 — idempotency/collision full-link-scan + TOCTOU around missing atomic graph-store op (HIGH); lifecycle DTOs exposing modes the impl rejects (HIGH); Qdrant tie-closure silently degrades, port cannot express completeness (HIGH); lifecycle policy lossy-projected onto Current/Active/Total stats (MED-HIGH); Option<bool> hint filters implemented as match-or-unknown (MED-HIGH); duplicated embed/index orchestration from unassociated Vec<Vec<f32>> port (MED); stats call sites re-owning graph hydration (MED); domain identity/order capabilities reimplemented ~9x + MemoryObjectRef/GraphObjectRef dual identity (MED); upper layers re-repairing lower-layer postconditions — double canonicalization, lifecycle re-filtering, SPARQL post-filtering (MED, R2-09); boolean trace/root/failure mode plumbing across layers (MED-LOW, R2-10); dormant governance/reconciliation vertical slice under #![allow(dead_code)] — deletion-first audit (MED, R2-11); speculative/dead vector query + source-reference APIs in production, GraphObjectQuery invalid-state product (LOW-MED, R2-12); implicit Qdrant payload contract via test-only constants + redundant record_type/dual text columns — wants one typed schema manifest (LOW-MED, R2-13); barrel/test-hook warning suppression (LOW, R2-14); one-to-one internal/public outcome clones with From-impl shims (LOW-MED, R2-15); duplicated integration-test setup (LOW, R2-16). Census: no TODO/FIXME/"workaround"/"hack"/"for now" markers exist anywhere in src/ or tests/; all guarded/dormant code is enumerated under R2-11/12/13/14. Audited non-findings recorded (PrepareOptions/CommitOptions booleans, degraded-results flag, Box forwarding, draft TryFrom boundaries).
5. Copilot deferrals recorded in the completed backcompat plan addendum: two pre-existing ADR-I-0018 edges (default_retrieval_object_types in models; RetrievalLifecyclePolicy in policy — the latter also touches R2-04); CME history_text prose-encoded structure (defer unless a parser appears).

## Cross-links for planning

- ONE port design pass: R2-03 (search completeness envelope) + CME r2 finding 2 (raw-vector capability port) + v0.1.6 embedded vector-recall draft (`draft/v0-1-6-embedded-vector-recall`) — design the vector port once.
- R2-02 (lifecycle modes) intersects v0.2 scoped-continuity lifecycle work — coordinate, don't duplicate.
- CM F-series verdict widening and CME round-1 propagation are producer/consumer halves of the same contract — design the verdict schema once, then report-schema evolution against sealed-reader constraints (report schema 1.0.0; sealed baselines stay readable per Compatibility Policy exemption).
- Small pull-forward candidates (user may want these early/independent): CME dead namespace-reset knobs; CM F1 rejection-half structured error.

## First post-merge item (USER-CONFIRMED 2026-07-21): Qdrant teardown hardening

Spec from the 2026-07-21 failure catalog: (1) on gRPC delete timeout, verify deletion via REST before failing (the deterministic delete-response-loss class — deletes commit, responses vanish in Docker Desktop transport on all routes); (2) pre-run orphan sweeper in test support pruning stale run-scoped collections (covers killed-mid-run processes that post-run cleanup can never handle; note CME uses cmem_eval_* naming, CM uses test_collection_*); (3) endpoint normalization to 127.0.0.1 in test support (IPv6-localhost fallback stall); (4) timeout caps calibrated to measured serial durations (write_planning legitimately ~353s). Retires the two-test teardown-transport waiver granted for the observability phase validation. Also consider: live-run mutex/scheduling for concurrent agent suites (contention wedged the collections API twice).

## Standing constraints

- Sealed artifacts byte-identical; sealed readers keep tolerance (ruled, in CME common.md).
- No backcompat shims anywhere else (Compatibility Policy, both repos).
- Workaround Tripwire rules now active in both repos; harness candidate staged (design_alerts field).

## Small follow-up (recorded 2026-07-22, from the determinism consult): correction-path divergence rejection

The correction path commits replacement objects/links via blind upsert; with deterministic IDs, a divergent same-version correction (same superseded set + index, edited content) is last-write-wins. STATUS UPDATE 2026-07-22: largely ABSORBED into the phase itself — the corrected content-identity ruling (re-consult after cm-reviewer's stateful defects) requires the pre-commit identity check and a self-supersession guard as execution companions of the deterministic replacement IDs. Residual for later: extending the same pre-commit divergence discipline to any remaining blind-upsert lifecycle paths, and the R2-01 v0.2+ ledger work it pairs with.

## Deferred latent (recorded 2026-07-22, from the evals-researcher census): operation-vs-attempt identity in lifecycle dedup

results.rs:414-423 asserts every occurrence of a lifecycle operation_id is structurally identical; a first-call outcome plus its converged-retry outcome (same operation_id, different graph fields under the round-4 repair semantics) would trigger ConflictingLifecycleOutcome and abort the summary — fail-closed, no double-count, and the current corpus cannot produce retries (census-verified: driver replays nothing, fixture corrections are distinct operations). Designing attempt-vs-operation identity properly belongs with the R2-01/idempotency-ledger v0.2+ work; special-casing empty-graph fields in the dedup would be a tripwire smell. DISPOSITION SUPERSEDED 2026-07-22: the round-4 repair semantics made converged retries real and the deferral invalid (caught by thesis-audit R3-01). RESOLVED IN-PHASE by the attempt-identity ruling: (operation_id, attempt_index) row identity, contiguity-checked shape-agnostic dedup, once-per-operation degradation counting, repair_attempt_count summary field. Nothing remains deferred here; the v0.2+ pairing note applies only to the CM-side idempotency-ledger work.

## Deferred input-side duplicate-collapse sites (censused 2026-07-22, evals-researcher at 31d4cfe)

The v2 ARTIFACT readers are being closed in-phase (four boundaries + shared raw duplicate validator). Input-side Value-mediated parses remain, censused and deferred: continuity fixture loader (fixture.rs:829-841, nested embedding Deserialize :117-159), enrichment/snapshot JSONL inputs (runner/enrichment.rs:26-42,61-77), flexible dataset loaders (locomo/longmemeval loader.rs — intentional tolerance), predictions parser (official_exports.rs:19-42). Evaluate per-site whether input tolerance is contractual (loaders) or a latent strictness gap (fixtures/enrichment).

## Tier A value-audit outputs (2026-07-22, APPROVED — no DELETE findings)

- OVERSIZED trim, schedule into the next correct_forget-touching task: MutationPlan.stats_projection_objects/links full parallel clones (correct_forget.rs:766-800) — replace with an omitted-ID filter at projection time; kills a 2x clone and a two-list drift invariant.
- When the v0.1.6 phase doc is authored, include a deferral-reconfirmation checklist: CanonicalCandidates envelope survival, R2-13 text columns, R2-03/R2-05 port findings, CME r2#2 — each parked consumer claim re-verified so "named consumer" stays real.
- Watch item: the CME mirror vocabulary (~1,200 lines, verdict.rs) is where dead weight would accrete first; the reader-strictness rule is now scoped to evidence boundaries to prevent manual-visitor proliferation.

## PR-15 exit-rubric deferrals (2026-07-23, user-directed loop termination)

Exit rubric applied to terminal Copilot comments: in-PR only for phase-delivered evidence-integrity defects or phase-introduced regressions; all else deferred here.
- bm25_only surface validation (config.rs:482): BM25 handles only episode/observation surfaces but generic validation admits policies selecting derived/threads/entities, silently ignored — the pre-existing sibling of the fixed vector_only selector gap. Fix shape: same policy-boundary typed rejection (VectorOnlySurfacePolicyError family precedent). Baseline-mode truthfulness, not phase scope.
