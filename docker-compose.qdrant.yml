services:
  qdrant:
    image: qdrant/qdrant:v1.19.0
    ports:
      - "6333:6333" # REST
      - "6334:6334" # gRPC (used by qdrant-client)
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
